/* 
Con WHERE hacemos consultas filtradas:

Se puede utilizar con operadores AND, OR, BETWEEN, NOT BETWEEN, IN, NOT IN, 
 =, !=, <, <=, >, >=, 

Y para columnas con texto: 
=, != o <>, LIKE, NOT LIKE, %, IN, NOT IN

Estructura: 

SELECT columna, otra_columna, …
FROM tabla
WHERE condicion
    AND/OR otra_condicion
    AND/OR …;
*/


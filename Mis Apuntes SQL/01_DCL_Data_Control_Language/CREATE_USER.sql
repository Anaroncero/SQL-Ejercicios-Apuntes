/* 
Para crear un usuario se usa CREATE USER 

Estructura:
    CREATE USER 'username'@'host' IDENTIFIED BY 'password';


Para conectarte con un usuario se hace desde consola o desde Workbench

Desde consola:
mysql -u usuario -p

Desde workbench:
Indicamos el nombre del la conexion, indicar el usuario y la contrasena, 
nombre del schema(base de datos, tiene que ser exactamente igual).

 */

